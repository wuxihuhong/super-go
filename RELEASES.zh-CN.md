# Super Go 1.0.0

**2026-08-31**

双棋种（象棋 + 围棋）桌面对弈工具的第一个正式版。本机人机、引擎互搏、第三方平台连线代打走同一条通路：同步局面 → 引擎出招 → 拟人化延迟 → 落子。

> English: [RELEASES.md](./RELEASES.md)

## 亮点

- **象棋开箱即用**：安装包随附 [Pikafish](https://github.com/official-pikafish/Pikafish)（2026-01-31）与 `pikafish.nnue`，无需再下引擎。
- **围棋本机对弈**：对接 [KataGo](https://github.com/lightvector/KataGo)（GTP）。19 路标准盘，中国 / 日本 / AGA 规则，可贴目、让子。
- **连线代打**：识别第三方平台棋盘，引擎代走或纯观战。象棋用 YOLO 识别；围棋用经典 CV（网格 + 交叉点分类），落子为一击交叉点。
- **3D / 平面棋盘可切**：默认立体木盘，可改平面 Canvas；深色 / 浅色 / 跟随系统；界面中 / 英 / 日。

## 象棋

- 棋力五档：等级分（1280–3133，`UCI_Elo`）/ 搜索深度 / 思考时长 / 节点数 / 不设限。
- 可调搜索线程与哈希表；对局中随时改执方与棋力。
- 引擎执红、执黑，或两边都开做互搏观战。
- 评估走势图（红方视角）、着法列表、引擎信息同栏。
- 悔棋、认输、暂停（严格轮替）、复盘跳转、终局悔棋复活。
- 走子 / 吃子 / 将军 / 终局音效（可关）。
- 行棋延迟：引擎算完后随机等待再落子（本机与连线共用，默认 0.3–0.9 秒）。

Windows 安装包带全部 CPU 变体，运行时优先命中本机指令集（如 `avx512icl`）。macOS 安装包带 Apple Silicon 版。

## 围棋

- 19 路；规则中国 / 日本 / AGA（贴目默认日本 6.5、中国与 AGA 7.5）。
- 棋力：访问量 / 思考时长 / 不设限。本版只挂一张主模型、满强度对弈，**不对人类段位放水**。
- 引擎执黑、执白或互搏；虚着、悔棋、认输、暂停。
- **算目**：工具栏「目」展示引擎结果（黑 / 白），不做本地数子、不做领地热图。
- **最佳选点**：盘上标出引擎候选（蓝 = 唯一最优，绿 = 接近正手）。
- 评估为黑方视角胜率 + 目差；闲时思考、宽根噪声可在设置中改。

**安装包不内置 KataGo。** 请先自行安装（macOS 可用 `brew install katago`），或在设置里指定可执行文件、模型与配置。留空则自动探测 Homebrew / `engines/go`。

## 连线

- 选中目标窗口后开始：识别局面并重开一局，默认可先观战，再在工具栏让引擎接管一方。
- 失败可自愈：点不中会重试；耗尽后进入「待人工介入」，在平台上手走一步即可恢复。
- 急停：`⌘/Ctrl+Shift+X`。
- 窗口置顶，方便压在平台旁边（不要挡住对方棋盘，否则抓不到画面）。
- macOS 首次连线会引导屏幕录制 / 辅助功能 / 输入监控授权。
- **macOS 落子必须目标窗口可见**（真实鼠标点击）。Windows 可选用后台捕获 / 后台落子。
- 平台走子有动画时请打开「动画确认」，避免扫到半途帧。

## 界面与系统

| 项 | 说明 |
| --- | --- |
| 平台 | macOS 12+（Apple Silicon，`.dmg`）· Windows x64（NSIS 安装包） |
| 主题 | 浅色 / 深色 / 跟随系统 |
| 语言 | 中文 / English / 日本語 |
| 快捷键 | 新对局、悔棋、认输、虚着、算目、最佳选点、暂停（空格）、置顶、连线急停等；mac 一律 `⌘⇧` |

安装包版本号为 `1.0.0-yyyyMMdd`（构建日）。

## 已知限制

- KataGo 及其网络权重**不随包分发**；未安装或未指定路径时围棋引擎无法启动。
- 围棋本机界面目前是 **19 路**（内核已支持 9 / 13 路）。
- 不做棋谱导入导出、开局库、领地热图、全谱批量分析。
- 象棋循环棋规（长将等）由引擎内部裁决；本软件终局以绝杀 / 困毙为准。
- 连线依赖屏幕识别，目标棋盘须完整可见；不同平台 UI 风格可能影响识别率。
- 安装包未签名：macOS 需右键打开；Windows 可能被 SmartScreen 拦截，选择仍要运行即可。

## 许可证

源码以 **GNU GPLv3 或后续版本** 发布，见 [LICENSE](./LICENSE)。第三方引擎、权重与识别模型的条款见 [NOTICE.md](./NOTICE.md)。

要点：随包 `yolov11.onnx` 来自 TCHESS（GPLv3）；`pikafish.nnue` **未经许可不得商用**。
