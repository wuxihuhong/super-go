# Super-Go 详细设计

> 双棋种（围棋 + 象棋）对弈工具的架构设计文档，由技术选型讨论收敛而来。

## 1. 项目定位

**做一个"下棋工具"，不是分析工作台。** 两种核心场景共用一条引擎通路（同步局面 → 要一步棋 → 拟人化延迟 → 落子）：

1. **本机人机对弈**：引擎执一方，难度可选（等级分/段位）。
2. **连线代打**：屏幕识别第三方平台棋盘 → 同步局面给引擎 → 引擎出招 → 模拟鼠标自动落子。

连线是灵魂功能，必须保留。

### 非目标（明确砍掉或无限期后置）

| 功能                                 | 处置                  | 理由                            |
| ------------------------------------ | --------------------- | ------------------------------- |
| ownership 领地热图                   | 后置                  | 分析工作台属性，与"下棋"无关    |
| 全谱批量分析（KataGo JSON 分析模式） | 不做，留 adapter 口子 | 同上，将来想要再加              |
| 首启配置向导                         | 不做                  | 静默默认值 + 设置逃生口取代     |
| UCCI 老引擎支持                      | 砍掉                  | 只做 UCI + GTP                  |
| 极致包体优化                         | 不追求                | Electron 的重在工具类场景可接受 |

### 保留的轻量分析

- **胜率条/形势判断**：每步后跑一瞬分析即可，下棋界面标配。象棋显示分数优势，围棋显示胜率 + 目差。
- **算目**：工具栏只展示引擎 `final_score` / 快分析；本次未算出则用上次引擎结果。用「黑 / 白」不用 W/B；不展示本地数子。双虚着终局兜底仍走规则层数子。不做领地热图。

## 2. 技术选型（已定案）

| 决策点       | 结论                                                                                               |
| ------------ | -------------------------------------------------------------------------------------------------- |
| 技术栈       | **Electron + TypeScript**（全 TS，单语言）                                                         |
| 跨平台       | Windows + macOS 优先，Linux 顺带                                                                   |
| 引擎         | 象棋 **Pikafish**（UCI）、围棋 **KataGo**（GTP），均为出厂预置，roster 对用户开放                  |
| 连线原生能力 | nut.js（全局鼠标监听 + 注入）、koffi 调 Win32 PrintWindow（后台截窗）、desktopCapturer（常规截屏） |
| 推理         | onnxruntime-node（官方包）                                                                         |
| UI 样式      | Tailwind CSS + 无头组件（Radix/shadcn 风格）自建轻组件集；棋盘用 Canvas                            |
| 参考实现     | Sabaki（Electron 围棋 GUI）可参考 GTP 对接与棋谱架构                                               |

选型过程的关键结论（防止反复）：

- Flutter 自绘体验最好但原生胶水全自己写；Tauri 要付 Rust + Swift 两笔学费；**Electron 五项原生能力全部有现成轮子**，对单人项目"能抄作业"价值最大。
- 无论壳怎么选，原生能力必须收进 `LinkerService` 端口层后面（见 §6.2），把平台耦合封顶在一个模块里，将来可换壳。

## 3. 总体架构

三层结构 + 进程模型：

```
┌─────────────────────────────────────────────────┐
│  Renderer（TS, UI）                               │
│  棋盘渲染 / 对局控制 / 引擎管理 / 设置 / 连线交互    │
└───────────────▲─────────────────────────────────┘
                │ IPC（事件推送 + 命令调用）
┌───────────────┴─────────────────────────────────┐
│  Main（TS）                                       │
│  ┌───────────────────────────────────────────┐  │
│  │ adapters                                   │  │
│  │  UciAdapter(Pikafish) / GtpAdapter(KataGo)│  │
│  │  ManualAdapter(SGF)                        │  │
│  │  RecognitionAdapter(象棋 YOLO / 围棋经典 CV) │  │
│  │  LinkerService（原生能力端口）               │  │
│  └───────────────┬───────────────────────────┘  │
│  ┌───────────────┴───────────────────────────┐  │
│  │ core（纯逻辑，无 UI，无 Electron 依赖）      │  │
│  │  Game 接口 + XiangqiGame / GoGame          │  │
│  │  MoveTree（通用着法树）/ 对弈状态机          │  │
│  │  规则 / 记谱 / 序列化 / Zobrist             │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

- **core 不依赖 Electron**，可在 Node 单测中直接跑。
- 引擎进程、识别推理、原生 IO 全部在 main 进程；数据经 IPC 事件推给 renderer。
- kata-analyze 每 100ms 一帧（几 KB/帧）对 IPC 无压力，不需要 utilityProcess。

## 4. 领域内核（core）

### 4.1 Game 接口

双棋种共同实现一套接口，**接口设计必须同时对着两个棋种验证**（只按象棋设计、围棋一进来就崩，是此类重构最常见的失败模式）：

```ts
interface Game<M extends Move, P extends Position> {
  readonly kind: 'xiangqi' | 'go';
  readonly boardSize: { width: number; height: number }; // 9×10 / 19×19(13,9)
  initialPosition(...setup): P;
  legalMoves(pos: P): M[];
  isLegal(pos: P, move: M): boolean;
  apply(pos: P, move: M): { position: P; captured?: Point[] }; // 围棋返回提子列表
  serialize(pos: P): string; // FEN（象棋）/ 紧凑局面串（围棋）
  parse(text: string): P;
  moveToNotation(pos: P, move: M): string; // 中文纵线格式 / GTP 坐标
  // 连线与终局需要
  diffPositions(before: P, after: P): PositionDiff; // 见 §6.4
  isGameOver(pos: P, history): GameResult | null; // 围棋双虚着/认输；象棋绝杀/困毙
}
```

要点：

- **Move 是多态的**：象棋 = from→to 两坐标；围棋 = 单点落子或 pass。
- **diffPositions 是一等公民**：连线识别的核心原语，象棋 = 一子两格；围棋 = 落一子 + 提子消失（返回"新增点 + 消失点"），围棋提子判断结合本地算气。
- 围棋需支持 9/13/19 路、贴目、让子、规则集（中式/日式等，影响劫与终局）。
- 象棋规则完整实现：走子、蹩马腿、对脸、送将、将帅照面、绝杀/困毙判定。

### 4.2 通用着法树 MoveTree

- 每节点：着法、局面快照（惰性计算 + Zobrist 缓存）、`EvalRecord`（多态评分：cp | 胜率+目差，人机/连线/胜率条的数据都挂这里）、注释。
- 支持分支变着（SGF 是树不是列表）、悔棋、跳转、主干重排。

### 4.3 对弈状态机

管理：轮到谁、先后手、让子/让先、时钟、认输/求和、引擎执方、难度档生命周期（见 §5.5）。

## 5. 引擎层

### 5.1 进程管理

引擎子进程归 main 进程 `child_process` 直管，按行解析，事件推给 renderer。stdout 解析保护性编程：info 行 schema 在引擎间有变体，解析失败降级不崩。崩溃监测 → 自动重启 → 快照重同步（§5.8）。

### 5.2 协议适配器

两个 adapter，一个统一接口 `EngineAdapter`：

```ts
interface EngineAdapter {
  launch(desc: EngineDescriptor): Promise<void>; // 含握手探测
  syncPosition(snap: PositionSnapshot): Promise<void>; // 任意局面直接摆（快照式）
  genmove(constraint: MoveConstraint): Promise<Move>; // 时间/强度约束
  startAnalysis(opts): void;
  stopAnalysis(): void;
  on(event: 'line', cb: (l: EngineLine) => void): void;
  setStrength(s: StrengthSpec): Promise<void>;
  on(event: 'exit', cb): void; // 崩溃监测 → 重启 → 重同步
  quit(): void;
}
```

**三处不对称，在 adapter 内部抹平：**

1. **局面同步策略**：UCI 无状态——全量重发 `position fen ... moves ...`，天然支持任意跳步；GTP 有状态——`clear_board` + 重放，**不依赖 `undo`**（各引擎实现参差）。围棋从快照摆盘：KataGo 的 `kata-set-position` 类扩展命令（以实际版本文档为准），否则由连线模块维护着法历史重放。接口统一为快照式 `syncPosition`，别假设总有历史。
2. **评估语义**：象棋厘兵（走子方视角，显示时换算红黑视角）；围棋胜率 + 目差。`EngineLine.score` 做成 `ScoreKind: cp | winRate` 多态，挂到 MoveTree 的 EvalRecord 上，胜率条/分数曲线复用同一结构。
3. **选项模型**：UCI 运行时 `setoption`；KataGo 核心配置在启动 config，运行时靠调用参数（`kata-analyze maxVisits N`）和 `kata-set-rules` 等。选项分"运行时可调"和"重启生效"两档，UI 标注。

### 5.3 KataGo 接入（路线 A：GTP 单进程，已定案）

- 对弈：`boardsize / komi / clear_board / play / fixed_handicap / genmove`。
- 分析：`kata-analyze interval 0.1 maxVisits N`（ownership 默认不开，热图后置）。
- 规则：`kata-set-rules`（中式/日式/AGA）；让子：`fixed_handicap`。
- 计时：`time_settings / time_left`。
- **双模型挂载**：`katago gtp -config <cfg> -model <强网> -human-model <b18c384nbt-humanv0>`，一个进程同时具备超人类强度与拟人段位出棋（需 KataGo ≥ 1.15.1）。
- JSON 分析模式：不做，Adapter 层留位。

### 5.4 Pikafish 接入

- 标准 UCI：`uci/ucinewgame/isready/position/go/stop/setoption` 全套。
- 关键选项：`Threads / Hash / MultiPV / EvalFile / Repetition Rule`（中规 ChineseRule ↔ 亚规 AsianRule）。
- 计时：`go wtime btime winc binc`；固定时间/深度/节点数出招。
- 分数为走子方视角厘兵；着法 ICCS 坐标（如 `h2e2`），坐标转换处理棋盘翻转。

### 5.5 强度系统（核心设计）

**产品语言：象棋问"要多少分的对手"，围棋问"要几段的对手"。** 两边引擎原生说人类标尺，GUI 只做映射不发明。

|        | 象棋（Pikafish）                                             | 围棋（KataGo）                                    |
| ------ | ------------------------------------------------------------ | ------------------------------------------------- |
| 第一档 | **UCI_Elo 1280–3133**（天梯校准）                            | **模型选择**：Human SL 模型（20k–9d 可配）vs 强网 |
| 细调   | Skill Level 0–20（粗档，同一机制）                           | maxVisits（仅在强网无限制档附近用）               |
| 预设   | ~1400 / 1800 / 2200 / 2600 / 2900 / 不设限，允许直接填目标分 | 级位段位直选（20k–9d → Human SL rank），之上强网  |

机制事实（设计依据）：

- 皮卡鱼弱化 = 出招时从候选按概率偏向较劣一路（内部自动 MultiPV=4），**只影响出招、分析输出诚实**——放水对弈与真实拆棋可并存。UCI_Elo 与 Skill Level 同机制，前者更细；elo 与级别有官方换算（1280=SL0，3133=SL19）。
- 强网 + 压 visits 是"犯低级错误的超人"，棋感假；拟人弱棋的正解是 Human SL 模型（官方用人类对局训练，可指定段位，甚至模仿历史年代棋风——后者作为彩蛋后置）。
- **弱化选项生命周期绑定对局，不绑定引擎**：默认满强度（`UCI_LimitStrength` 关、Skill Level 不碰）；用户显式选限制档才下发 `setoption`；对局结束或切回分析立即复位。放水选项是粘滞的，忘记复位会污染分析数据——上下时机由对弈状态机统一管理。

### 5.6 引擎分发与默认值

**静默给默认，设置留逃生口，不做向导：**

0. **随包分发（2026-08-25 已做，象棋侧）**：安装包经 `extraResources` 携带引擎——mac 带 apple-silicon 版、Windows 带全部 CPU 变体（avx512icl 优先命中，如 Zen5；逐级回退到老 CPU 可用档），NNUE 与二进制同目录；运行时探测顺序：用户指定路径 > 打包 Resources/engines > 开发仓库。下述"自动下载"面向联网增量与 KataGo 等大体量分发，尚未实施。
1. 首次用到某引擎时后台自动：探测平台 → 下载引擎包 + 模型 → 内置模板生成 config → 启动。只有一个进度条，没有问答。
2. 探测规则（粗即可）：macOS arm64 → Metal 版；Win/Linux 且 `nvidia-smi` 跑通 → CUDA 版；其余 → Eigen 版。
3. **试启动回退链是比硬件嗅探更稳的主机制**：按最优猜测起进程，起不来（如 CUDA 版缺 DLL 立即退出）就沿 Metal/CUDA → OpenCL → Eigen 逐级回退。Eigen 版零依赖，永为兜底。
4. 默认下载模型：强网（b18 主力）+ Human SL（`b18c384nbt-humanv0`），KataGo 条目双模型。
5. 所有路径/参数在设置中可见可改；用户改过就永远用用户的，探测不再碰。
6. 下载地址可配置（内置镜像选项）——KataGo 在 GitHub Releases，国内直连慢。
7. 皮卡鱼单文件内嵌 NNUE，无回退链需求。

### 5.7 EngineDescriptor 与引擎设置

**描述符（复合模型）**：引擎 ≠ 一个可执行文件。KataGo 条目 = 可执行文件 + config 路径 + 主模型 + 附加 human 模型 + 后端标识；皮卡鱼条目 = 可执行文件 + 选项表。roster 开放：用户可挂任意 UCI/GTP 引擎，皮卡鱼/KataGo 只是预置项。

**引擎能力可设置（硬性要求）**：引擎的每一项能力/参数在引擎管理面板可查、可改。

1. **选项自动发现**：
   - UCI：握手时解析 `option name X type Y default Z [min/max/var]`，自动生成选项表（含引擎自定义项，如皮卡鱼的 Repetition Rule、EvalFile）。
   - GTP/KataGo：预置已知选项 schema（后端、线程、maxVisits、规则等），从内置模板生成启动 config；未识别的第三方 GTP 引擎给基础选项表。
2. **统一选项模型**：类型化（bool / int / string / enum / 路径 / 模型文件），携带默认值、范围、枚举值元数据；UI 按类型渲染对应控件（开关、数字输入、下拉、文件选择），不做裸字符串编辑。
3. **生效方式分档并标注**：运行时生效（UCI `setoption`、kata-analyze 调用参数）/ 重启生效（KataGo config 项、模型与后端切换）。修改重启生效项时提示一键重载。
4. **配置作用域**：引擎级（持久化，用户管理，如 Threads/Hash/规则）；对局级（强度、时限——对弈状态机管理，对局结束复位，见 §5.5）；会话级（临时调参不落盘）。
5. **能力协商驱动 UI**：握手后记录能力位（协议、MultiPV 支持、是否挂 human 模型、后端类型），UI 据此呈现——例如 KataGo 未挂 human 模型时，难度选择器隐藏段位档、只留强网 visits 档。
6. **用户值优先**：用户显式修改过的引擎级选项永久生效，程序默认值与探测逻辑不再覆盖（与 §5.6 一致）。

### 5.8 崩溃恢复

main 进程监听引擎 exit → 自动重启 → 用快照式 syncPosition 重同步当前局面 → 继续对局。全量重发/重放策略的红利。

### 5.9 闲时思考（ponder）

**定位**：对局级能力，**对局中随时可开可关**。职业棋手在对手回合本就持续思考——ponder 是比"空等 + 随机延迟"更真实的拟人：**延迟管落子节奏（§6.1），ponder 管等待期间的思考内容，两者正交**；ponder 命中的结果仍走拟人化延迟出招，不改变节奏。

- 棋种默认值：**两棋种默认关闭**，对局中随时可开。收益差异作为开启指引：围棋对手回合长、中盘复杂度深，开启收益大；象棋出招快、开局有库招覆盖，收益相对小。
- **UCI（Pikafish）实现——adapter 驱动，随时生效**：对手回合以引擎最近分析的 PV 首着为预测，发 `position ... moves <预测>` + `go ponder`；对手实际走子命中预测 → `ponderhit` 后停搜取招，未命中 → `stop` 弃树重搜。开关只是 adapter 是否发起 ponder，无进程代价。
- **GTP（KataGo）实现——重启生效档（§5.7）**：`ponderingEnabled` config + pondering 限额项。对局中切换的运行时命令**待核实**（`kata-set-param` 未收录 pondering 项，以 `kata-list-params` 实测为准）；fallback = 重启进程 + 快照重同步（复用 §5.8，代价可忽略）。
- 与弱化并存（§5.5）：弱化作用于搜索本身（UCI_Elo / Human SL 在 ponder 搜索期间同样生效），ponder 只延展搜索时间；若实测放水档位有明显漂移，再按档调整默认值。
- 附带收益：对手回合的 ponder 输出（UCI info 行 / 分析帧）顺带驱动胜率条与形势更新（§7.4），无需额外分析通路；引擎信息面板显示 ponder 状态。连线模式同一开关同一机制。

## 6. 连线模块（Linker）

### 6.1 流程

点选目标窗口 → 扫描循环（定时截屏 → YOLO 识别棋子 → 与本地局面 diff → 发现对方走棋）→ 引擎 genmove → **拟人化延迟**（出招延迟范围随机）→ 模拟鼠标点击落子 → 继续扫描。

**连线 = 重开一局（2026-08-25 定）**：连线启动即以平台识别局面开一局对弈（`newGame({ initialFen })`，快照式摆盘），对局本体（着法树、评估走势、悔棋、引擎执方与强度生命周期）全部复用 MatchService；连线模块只做两件事——识别对方着法喂给对局（playMove），引擎应招经**出招拦截器**按「本地棋盘先走 → 再点平台等它跟上」落子（2026-08-26 定：先给自己看棋，平台失败不回滚本地）。**本地棋盘方位跟随平台视角**（识别的 reversed 结果驱动翻转：对方红棋在平台哪边，本地红就在哪边）。引擎执红/执黑为工具栏常驻按钮，任何对弈状态（人机/连线/互搏）下随时换边（setEngineSide 同一通路）。中途接入看不出轮值时：引擎已执一方则该方行棋（象棋执红/执黑、围棋执黑/执白同一规则）；尚未选执方则先按红/黑先开局，用户点执方后再纠正。

支持模式：自动走棋 / 观战分析（只识别不操作，engineSide=null）。

### 6.2 LinkerService 端口（原生能力隔离层）

```ts
interface LinkerService {
  listWindows(): Promise<TargetWindow[]>;
  captureWindow(id: string): Promise<ImageData>; // Win: PrintWindow(koffi) 支持被遮挡; mac: 前台截屏
  captureScreen(rect): Promise<ImageData>;
  listenGlobalMouse(cb): Disposable; // 点选目标窗口用
  injectClick(pt: Point, opts: ClickOpts): Promise<void>; // 间隔/走子间隔可配
  infer(model: YoloModel, img: ImageData): Detection[];
}
```

**全项目唯一允许出现平台耦合的模块**；core 与 UI 只依赖此接口。

### 6.3 平台差异与"后台模式"的定位

**基线是前台视觉连线，两个平台一致**：屏幕识别 + 真实鼠标点击。这是唯一**不需要目标
配合**的手段——原生程序、浏览器、游戏、虚拟机、投屏过来的手机，什么都能连。代价也
由此而来：目标窗口必须可见且不被遮挡，落子会移动真实光标并把目标切成活动应用。
这不是实现没做好，是"通用"这个目标的必然定价。

**"后台"必须拆成两件独立的事**，它们的可行性完全不同，混为一谈会得出错误结论：

- **后台捕获**：直接取窗口内容而非截屏裁剪，被遮挡也能识别。**两平台都可用**。
- **后台落子**：不动光标、不要求窗口可见。**仅 Windows**；macOS 不存在这种能力。

**后台能力是加分项，不是基线**。通用性与后台能力天然互斥：越通用的手段越依赖"像人一样
操作屏幕"，越能后台的手段越依赖目标配合（浏览器要 CDP、AX 要语义元素、PostMessage 要
应用肯收合成消息）。因此后台通路一律做成**取不到就自动回落**的加速层，绝不让基线依赖它。

|                          | Windows                          | macOS                                        |
| ------------------------ | -------------------------------- | -------------------------------------------- |
| 基线（前台视觉连线） | ✅ | ✅ |
| **后台捕获** | ✅ PrintWindow（**本仓库未实测**） | ✅ `CGWindowListCreateImage`（已实测可行） |
| **后台落子** | ⚠️ PostMessage，挑应用（**本仓库未实测**） | ❌ **不可能**，见下 |
| 最小化（两者皆失效） | ❌ | ❌ |
| 权限 | 无特殊 | 屏幕录制 + 辅助功能 + 输入监控，首次引导授权 |

设置项对应 `LinkerSettings.backgroundCapture` / `backgroundClick` 两个独立开关；
`backgroundClick` 在非 Windows 平台由 UI 禁用并说明原因（不藏起来，让用户知道为什么）。
旧版合二为一的 `backMode` 只映射到 `backgroundClick`——它名不副实，win32 的截图一直走
PrintWindow、与它无关（迁移见 `migrateSettings`）。

> ## macOS 定论：后台落子不可能（2026-08-25，勿重走）
>
> 目标窗口**无焦点 / 被遮挡**时驱动它落子，在 macOS 上**做不到**。不是工程量问题，
> 是没有可用的公开 API。换开发语言也没用——`CGEventPostToPid` 是 C 函数，
> Swift/ObjC/Rust 调它和 koffi 调它是同一次系统调用。
>
> **证据链**（三类目标 × 四种 API 变体 × 一次工具自检，全部失败）：
>
> | 维度 | 覆盖 |
> | --- | --- |
> | 目标程序 | Java/AWT（TCHESS）、SceneKit 3D 游戏（Chess.app）、标准 AppKit（计算器） |
> | API 变体 | NULL 事件源 / `kCGEventSourceStateHIDSystemState` 源 / 加窗口路由字段 91、92 / 补齐 `eventNumber`+`pressure`+`subtype` |
> | 对照 | 同坐标下 nut.js 真实点击每次都成功（计算器那轮变化 21.6%） |
>
> **工具自检**（排除"是我们自己代码写错了"）：同一批事件对象、回读校验类型与坐标全部
> 正确，`CGEventPost(kCGHIDEventTap)` 生效，`CGEventPostToPid` **零像素变化**。
> 事件是好的，是投递通路不工作。
>
> **根因是操作系统对"输入"的定位不同**：Windows 把输入设计成**消息**——窗口有队列，
> 任何进程都能投递，`PostMessage` 天然就是"给某个窗口发点击"。macOS 把输入设计成
> **硬件流**——HID → WindowServer（独立特权进程）按命中测试与焦点决定给谁，应用没有
> 对外可寻址的事件队列；想控制别的程序走的是另一套**语义通道**。
>
> 准确说法不是"macOS 没有对应 API"，而是：
>
> | macOS 通路 | 性质 | 为何不解决问题 |
> | --- | --- | --- |
> | `CGEventPostToPid` | `PostMessage` 的**直接对应物**，公开 API | 存在，但**实测不送达**（本节证据链） |
> | Apple Events / AppleScript | 真 IPC，后台不动光标 | 只传应用**自己声明支持**的语义命令，棋类程序基本不可脚本化 |
> | 辅助功能 AXUIElement | 真 IPC，后台不动光标 | 按**语义元素**驱动，自绘棋盘在 AX 树里是一整块 |
> | XPC / 分布式通知 | 真 IPC | 要求目标**主动**开接口 |
>
> 直接对应物存在但不送达，能用的都要求目标暴露语义——而自绘棋盘对外只是一块像素。
> 于是第三方进程只剩全局 HID 通路（`CGEventPost`），其语义是"在屏幕某坐标真点一下"，
> 因此**必然移动光标、必然打给最上层窗口**。
> 一句话：**Windows 让你冒充鼠标，macOS 只让你请求应用做事**。
>
> 注：`CGEventPostToPid` 不送达的**原因**未知——是苹果主动收紧（10.14 后合成事件持续
> 加门槛），还是存在未文档化的前提未满足，我们只测出了"不送达"这个事实，不做推断。
>
> **已排除的替代路线**：
>
> - **辅助功能（AXUIElement）**——苹果官方的"驱动别的程序"通路，后台、不动光标、不抢
>   焦点。但它按**语义元素**驱动，而棋盘是自绘视图（Canvas / NSView / SceneKit），
>   在 AX 树里是一整块无内部结构的东西，没有"某一格"可抓。对通用连线不成立。
> - **私有 API（SkyLight `SLPSPostEventRecordTo` 等）**——唯一未穷尽的技术路子。不建议：
>   鼠标事件记录格式无公开文档，且每个 macOS 大版本都可能失效。注意否决理由**不是**
>   上架审核：本项目因 yolov11.onnx 已按 GPLv3 分发，与 Mac App Store 条款不兼容，
>   且沙盒拿不到屏幕录制+辅助功能+输入注入，**本来就只能走 Developer ID 签名 + 公证的
>   直接分发**，而公证只做恶意软件扫描、不查私有符号。否决理由是**易碎，不敢依赖**。
> - **虚拟显示器 / 虚拟机**——能达到"看不见"，但前者光标仍会跑过去、焦点仍会切；
>   后者要把目标搬进 VM。都不是通用方案。
>
> **后台捕获这一半是通的，已实现**：`CGWindowListCreateImage` 对 Java/AWT 与原生应用
> 都能抓到完整窗口图，**不要求目标是活动窗口**（已实测），机制上不依赖 z 序。
> 价值不在"后台落子"（落子仍需可见），而在**识别不再怕短暂遮挡**——通知横幅、输入法
> 候选框、本 app 自己的面板，这些此前会直接毁掉一帧。
> 实现在 `capture/macWindowCapture.ts`：取不到内容（最小化 / 系统版本变化导致符号缺失）
> 时返回 null，自动回落到截屏裁剪并提示一次，不中断连线。

### 6.4 双棋种识别差异

|              | 象棋                   | 围棋                                                                                         |
| ------------ | ---------------------- | -------------------------------------------------------------------------------------------- |
| 识别         | YOLO 14 类棋子 + 棋盘框 | **经典 CV**（等距线簇网格标定 + 交叉点圆盘中值分类），零模型（2026-08-29 定）                 |
| 局面 diff    | 一子两格               | 落一子 + **提子消失**（diff 非局部，结合规则引擎算气校验；同色连落推断插入 pass）              |
| 自动落子点击 | 两下（起点+终点）      | 一下交叉点（虚着不点盘；截图/点击复用同一 LinkerNative，Win/mac 两套通路不动）               |
| 合法性防线   | 本地规则校验拦截误识别 | 同左，另加劫争校验                                                                           |

**围棋识别方案（2026-08-29）**：场景是数字渲染窗口截图，不是照片。社区同场景（野狐连线生态的网格校准 + 交叉点亮度阈值）证明经典 CV 足够稳；现成深度学习权重（如 moku）训练域是实体棋盘照片，不适合。自训 YOLO 已排除。readboard 无许可证，只借鉴思路、代码自研。9/13/19 路由线数自动判定（19 路盘上线峰不齐时禁止把中间 13 路子集当成终局）；圆盘中值抗最后一手标记与星位。浅色暖盘（野狐）白子按去黄（r−b）与木纹区分，不要求比盘面更亮。中后盘格线被挡时改用棋子质心拟合网格，分类底色取盘边木纹（避免成片同色子把中间认成空）。整窗截图（棋盘 + 深色分析栏）先裁木纹盘 ROI 再标定，避免侧栏污染线投影与质心。木纹外框与底栏播放按钮不得当成第 1/19 路。首末路是否在控件上要看出界一侧：出界仍是木纹则是真成片黑子，禁止整网误挪一格。网格只标定一次（后续帧走 `goGridStillValid` 校验内容，不合格才重标）。识别 unknown 超限、re-arm、resync 会失效标定。整窗密盘首次标定可在木纹框内搜 19 路内缩：盘框优先取**最大暖色连通域外接框**（官子收完的满盘棋子盖住 95% 木纹，行列密集带会塌成局部空木纹区；但棋子盖不住外圈边距，边距+子缝+空区连成一片，外接框仍是完整棋盘），退回密集带框；内缩从 0 起扫（野狐格线几乎贴住木纹边缘，从 5% 起扫会整体错过真实几何），以黑白子像素掩码的「果断子色覆盖率」计分（锚点落在子缝上时覆盖率塌掉），再按格点/子缝相位（沿轴子密度梳齿相关）精修原点与步长；计分限制在盘框内，防止伸出盘外的网格点吃深色底栏刷分。空盘/9/13 占子不够则仍走线检。网格调试输出由 `SUPER_GO_GRID_DEBUG=1` 开启。自训 YOLO 与 moku 继续排除。识别器按棋种分派（象棋=YOLO，围棋=CV），截图与点击一律走已有 LinkerNative。

**围棋连线与象棋同一主流程**：识别 → 以平台局面重开一局（默认观战）→ 工具栏执黑/执白接管 → 引擎 genmove → 本地先落子 → 一击交叉点等平台跟上。提子/劫/pass 由 `goDiff` 解释；点击注入复用象棋 LinkerNative。开局/重开写入用户设置的贴目与规则（不默认写死中国规则 7.5）。象棋连线先加载 YOLO，成功后再 `setKind`，避免模型失败却已切棋种。

### 6.5 连线参数

鼠标点击间隔、走子间隔、扫描间隔、识别线程数、动画确认开关（目标平台棋子有无移动动画，有动画必须开否则误识别）。**行棋延迟范围**属象棋引擎固有配置（`xiangqi.moveDelayMin/MaxSec`，0–15 秒）：本机人机与连线出招共用，引擎算完后、落子前随机等待；两端都为 0 则立即走。从旧 `linker.moveDelay*` 升级时只迁非 0 值（旧默认一律写出过 0–0，迁过去会让全员本机变成立即走）；未迁的走 0.3–0.9。

> **⚠️ 点击类默认值（clickHoldMs / clickBetweenMs）未经真机验证**：它们标注为"取自 TCHESS
> 实战值"，但直到 2026-08-25 修好点击坐标之前，落子路径根本没在真机上正确跑通过，
> 这些值不可能是在能命中的前提下调出来的。真机跑顺后需要重新校准。

### 6.6 坐标标定（点击落在哪）

三段换算：检测框反 letterbox → **棋盘网格** → 屏幕坐标。

- 网格是一等概念，**不等于 `'0'` 棋盘框**：框覆盖 9×10 交叉点区域，可由它推出粗网格，
  但框本身有系统偏差（TCHESS 实测格距比真值大 2~5%，且随取景抖动）。
- **棋子中心自标定**：粗网格吸附一遍后，用棋子中心做最小二乘拟合出真实网格，再吸附一遍。
  退化保护（子数 < 6 / 单轴跨度 < 4 格 / 内点率 < 80% / 与粗网格偏离 > 15%）任一触发即退回粗网格；
  取景不变时跨帧 EMA 平滑。实测点击点与棋子中心偏差 ≤ 0.02 格（仅靠框约 0.2~0.3 格）。
- **点击点就是格点本身，不做任何"外扩/内收"补偿**——外扩 N 格再取格心在数学上是恒等变换。
  历史教训：曾把"外扩 0.8 格"实现成"缩小格距"，点击点向盘心收缩，边线最大偏 0.98 格
  （几乎正好落在相邻交叉点上），表现为"中路准、边线错"。回归防线见 `geometry.e2e.test.ts`。
- **截图与点击必须同源**：抓帧时按窗口当前位置取景，并把换算基准（ClickAnchor）随帧返回，
  点击时用回同一个基准；否则用户拖动平台窗口就会让点击整体偏掉一个位移量。
  围棋锁网后禁止在落子前重标（重标会把密盘认偏、点击整网错位）。引擎思考与拟人延迟
  期间扫描循环只刷新锁网与 ClickAnchor、不解释着法，落子用最近一帧的同源网格+原点。

### 6.7 开局基准（局面同步的地基）

连线的整局对局都建立在**开局那一帧**上，所以这一帧的标准比扫描期更高：

- **必须是稳定帧**：连续两帧识别盘一致才可用。平台走子动画播到一半、或某枚棋子
  置信度抖一下，那一帧就会少一枚子；拿它当基准，此后每一帧都对不上
  （真机现象："局面同步不准确"）。扫描期对方着法有 `animationConfirm` 兜底，
  唯独决定基准的这一帧此前没有，是设计缺口。
- **本地一步未走却与平台对不上 = 基准识别错了**：着法树是空的，没有历史可丢，
  直接以平台局面重开，不必拿纯内部失误去打扰用户；重开次数有上限（防空转），
  超限才转 `attention`。已经有着法之后再对不上，才是需要用户决断的情况。
- **漏子比多子更危险**（2026-08-25 真机实证）：平台画在棋子上的覆盖物（走子箭头、
  选中角标）会把类别置信度压散——真机一帧里黑炮的正确类别只剩 0.310，被旧的 0.5 阈值
  整枚漏掉，于是整局建立在少一枚子的局面上，且前后帧自洽、**不报任何错**。
  多出来的子有同格取高分、尺寸/框内过滤、盘面静态校验、着法合法性四道防线拦截，
  漏掉的子却能安静地产出"看起来合法"的错局面。故阈值分两档：棋盘框 `'0'` 保持 0.5
  （被低分伪框劫持则整帧坐标系作废），棋子放宽到 0.25。该帧已固化为 fixture
  `tchess-overlay.png`。
- 诊断：`SUPER_GO_LINKER_DIAG_DUMP=<目录>` 把整帧原始像素 + 检测结果落盘
  （raw RGBA + JSON 边车），用于离线复现识别掉子——真机上抓不到的帧只能让 app 自己交出来。

### 6.7 失败恢复与人工介入

原则：**先尽力自愈，自愈不成请人工，绝不无声终止**。

引擎应招落到平台的闭环是"本地先落子 → 点击平台 → 等平台跟上"。
点击成功只代表注入通道可用，不代表平台接受了这一步；确认失败不回滚本地，
由 pending-sync 重试或人工在平台上走齐。

自愈：重新抓帧标定（网格/基准过期是最常见的假失败）→ 退避重点 → 重新确认，共 3 轮。

自愈耗尽 → 进入 `attention`（待人工介入）：

- 连线**不退出**：继续识别扫描；只冻结引擎、暂停自动落子；
- 面板按**结构化原因码**给出原因 + 建议 + 决断按钮（重试自动走子 / 以平台局面重开 / 转为观战）；
  「重试自动走子」退出待介入后立刻再点本地已超前、平台尚未跟上的那一步，不等下一轮扫描碰巧扫到；
- **人工接管是一等路径**：用户直接在平台上把这步走掉，识别到即自动恢复自动走子。
  为此 MatchService 提供 `playObserved`——连线中**平台是事实源**，绕过"轮到引擎"与
  thinking 门禁（仍走规则校验）。
- 退出条件按原因区分：`boardLost` 要等棋盘重新可见；其余原因在两边局面重新一致时解除
  （走子失败时本地已超前，必须等平台跟上，不能一进 attention 就因「本地已有这步」立刻解除）。

原因码：`clickChannel`（注入通道不可用）、`platformUnresponsive`（平台不走这一步）、
`boardLost`（连续识别不到棋盘——目标被遮挡/最小化；恢复可见即自动继续。**本 app 自己的
窗口压住平台**也算，macOS 前台截屏没有例外）、`boardMismatch`（局面无法互相解释）、
`engineUnavailable`（开局失败，此项直接终止——没有本地对局，人工接管无从谈起）、
`crashed`、`user` / `shortcut`（正常停止，不报警）。

沿用上一张好帧可以扛住偶发丢帧，但不能无限沿用——那是"卡住却什么都不说"。

局面对不上时**不擅自清空着法树**：只有识别盘 = 标准初始局面（平台明显开了新局）才自动重开，
否则转 attention 由用户决断。

## 7. UI 层

### 7.1 视觉设计原则（硬性要求：赛博 HUD）

- **用色**：暗底为默认骨架，青色主强调 + 洋红次强调；彩色承担数据语义（黑/白胜率、红方优势、连线正常、危险态），不做额外装饰色。亮色主题同步维护同一套 token。
- **信息密度分级**：默认低密度——顶栏只留棋种 / 引擎 / 窗口级开关；主操作下沉到棋盘下沿浮动 dock；引擎思考细节与着法进侧栏，不设底部状态栏。
- **HUD 层次**：圆角 + 细描边 + 克制光晕（暗色 `acc-glow`，亮色降级为投影）；棋盘区可有扫描线 / 扫掠光（`view.hudSweep`，默认开），`prefers-reduced-motion` 时关闭。
- **排版**：显示字体 Chakra Petch，数字 / 遥测 / 着法一律 JetBrains Mono + tabular-nums；CJK 回退 PingFang SC / 微软雅黑。
- **留白**：间距统一节奏（4/8 网格），侧栏定宽 348px（可拖 300–480）。
- **暗色主题为一等公民**：长时间对弈夜间居多，暗色默认打磨到位，亮色同步维护。
- **棋盘与棋子**：2D Canvas / 3D Three.js 渲染逻辑不变，只消费主题 token（盘面 / 棋子 / 最后一手 `--m-last`）；质感属棋盘内容数据域。窗口：mac `hiddenInset`；Windows 自绘 32px 标题栏 + WCO 系统按钮。棋盘上下沿不做双方信息条（行棋方由棋盘方位表达，身份信息收进顶栏与侧栏）。

### 7.2 技术实现

- UI 壳：Tailwind CSS + 无头组件（Radix/shadcn 风格）自建轻组件集，不引重型组件库，观感可控。
- 棋盘双模式（2026-08-25 定）：
  - **3D 棋盘（Three.js，默认）**：透视固定取景（57° 俯角，对弈视角不可拖拽/缩放）、车削轮廓棋子（LatheGeometry）+ 阴刻字顶盘（clearcoatMap 漆面遮罩）、程序木纹 + 刻线凹槽 + 炮/兵位折角标记、实时软阴影、按需渲染（静止时不耗 GPU）；执黑时贴图整体旋转 180°。WebGL 初始化失败自动回退 2D。
  - **2D 棋盘（Canvas）**：按 devicePixelRatio 高分屏渲染，与 3D 消费同一套语义 token；设置中可切换（`view.board3d`）。
- 动效克制：走子/落子带缓动位移，提示标记低透明度；禁用弹跳、发光等花哨效果。
- **浏览器开发模式**（2026-08-25 增）：`dev:web` 独立命令独立端口，无 preload 时注入 mock API（core 真规则 + 模拟引擎 + 全 UI 状态机），UI 迭代与调试脱离 Electron 进行。

### 7.3 布局与控件

- 单主窗口：窗口层（mac 52px / Windows 32px 标题栏 + 52px 命令栏）+ 棋盘区 + 右侧可折叠侧栏（默认 348px，左缘可拖拽 300–480 并持久化；窗口宽 < 1100px 自动收起，手动展开覆盖棋盘）。无底部状态栏。
- 顶栏：棋种切换、mac 居中品牌、引擎状态 chip（对局中点开棋力临时配置）、引擎执方、窗口工具组（连线 / 缩放 / 侧栏 / 设置齿轮 / 置顶）。
- 浮动 dock（棋盘下沿）：新对局 / 悔棋 / 暂停；围棋另有虚着 / 最佳选点 / 算目，象棋另有关于 / 设置；末项认输（二次确认）。按钮不标键位，快捷键走 400ms tooltip。
- 快捷键不变：新对局 / 悔棋 / 认输 / 虚着 / 最佳选点 / 算目 / 对局 / 引擎执先·执后 / 连线 / 棋盘缩放 / 侧栏 / 设置 / 窗口置顶。**mac 一律 ⌘⇧**；Windows/Linux 为 Ctrl（认输仍 Ctrl+Shift+R）。暂停为 Space。连线急停仍为 ⌘/Ctrl+Shift+X。
- **关于窗口**（居中卡片）：产品名、双棋种标记、版本、联系邮箱、GitHub、GNU GPLv3。入口：象棋 dock「关于」、设置「通用」页、mac 标题栏品牌与应用菜单「关于」。
- **棋力是固有配置**：设置面板持久化；对局中通过引擎 chip 打开的「对局」面板即时调整。
- 对话框最小化：设置用内嵌页，仅保留必要的文件选择/确认弹窗。
- 棋盘尺寸自适应（按中央区实测高度推算；底部预留 96px 给 dock）。

### 7.4 对局中的反馈

- **侧栏仪表**：顶部胜率 / 红方优势条承接原底栏评估；遥测行展示引擎 / 状态 / 强度 / 深度或访问量 / 目差或优势（只用现有 IPC 字段）。
- **评估走势折线图**：侧栏底部可折叠单线（围棋青色、象棋洋红）；x = 手数，y = 胜率偏差或红方整数分，均势虚线基准，杀棋钳制满幅；实时思考帧仅思考中生效。
- **走子标记**：起点实心圆点 + 终点取景框式四角角标（无连线——不穿棋子、一格短走不丑；角标半径大于棋子，3D 立体棋子也盖不住）；选中环（圆）与走子角标（方）形状区分。
- 最后一手标记、引擎思考中的呼吸点指示；将军以棋盘将军环 + 状态行 + 音效表达。
- **最佳选点**（围棋）：仅引擎上场时展示；停止连线或引擎离场（执方双关 / 转观战）即关闭开关并清掉盘上选点。
- 音效（可关）：走子/吃子/将/终局（WebAudio 合成）。

### 7.5 国际化与主题

**i18n**：

- 语言范围：**中 / 英 / 日** 三语；默认跟随系统语言（不在范围内时兜底中文），设置中可手动切换并持久化，即时生效无需重启。
- 文案架构从 P0 起就位：所有 UI 字符串走路由键 + 按语言分文件的资源包，**禁止硬编码文案**（后期返工成本极高）；三语翻译内容可后置补全。
- 记谱与棋类术语（中文纵线格式、GTP 坐标、贴目/劫/提子等）属领域数据，不随界面语言切换。
- 字体栈补 CJK 回退（中文 PingFang SC / 微软雅黑，日文 Hiragino Sans / Meiryo），排版规则（4/8 网格、tabular-nums）不变；日文文案注意按钮/标签宽度弹性。

**主题**：

- **浅 / 深两档 + 跟随系统**三种状态，默认跟随系统（Electron `nativeTheme`），手动选浅或深则固定并持久化，随时可改回跟随；切换即时生效。
- 颜色全部走语义 token（CSS 变量 / Tailwind 语义色），浅深两套 token 同源定义，禁止组件内硬编码色值；**Canvas 棋盘消费同一套 token**，与整体主题联动。
- 衔接 §7.1"暗色一等公民"：两套主题同步维护、同步验收。

## 8. 棋谱与开局库

- **统一 MoveTree 为底座**。
- 象棋：**不做棋谱导入导出**（2026-08-24 确认砍掉，用户不需要；PGN/XQF/CBR 一律不做，将来真有需求再评估）。
- 围棋：SGF 完整支持（树、变着、注释、标记——这是 SGF 的日常形态，非可选）。
- **开局库是象棋特性，围棋不做**：KataGo 自我对弈已覆盖布局与定式，Human SL 模型自带人类开局风格，外挂人类定式库反而削弱棋力（KataGo 官方也只对小棋盘提供开局书，19x19 无亦无计划）。象棋侧详细设计见 §8.1。

### 8.1 象棋开局库（本地库部署安装）

**产品形态**：开局库是与引擎同级的可管理资源——预置、下载安装、用户导入、启停与优先级，全部在**开局库管理面板**（设置内嵌页，与引擎管理面板同风格）完成。**只有本地库招，无在线查库能力**——下载安装的也是库文件，装完即本地资源，运行时不联网。

**库源三种**：

| 来源     | 说明                                                                                                                                |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 预置库   | 随应用打包的精选库（常见布局 15–20 步深，MB 级），开箱即用                                                                          |
| 下载安装 | 内置下载源 + 国内镜像，大库（亿级局面）按需安装；下载的是库文件，复用引擎分发机制（§5.6：静默默认 + 设置逃生口 + 用户装过不再覆盖） |
| 用户导入 | 支持社区格式 OBK（兵河五四）/ PF（鹏飞 .pfBook）/ XQB（鲨鱼），三者本质均为 SQLite，导入时离线转换为内部格式                        |

**内部统一格式（自有，SQLite）**：

- 局面主键 = 规范化 FEN（回合计数等易变字段清零）+ **镜像归一化**：上下/左右对称局面映射到同一 key，命中后着法反向还原（XQB 的位压缩键已验证此做法；OBK/PF 用 Zobrist 哈希，需左右镜像双查两次且存在整数/浮点双精度编码坑）。**存完整局面而非 Zobrist 哈希**——与快照式 syncPosition 同哲学，规避哈希碰撞。
- 每条记录：着法（ICCS 坐标）+ 评分（归一到 ScoreKind）+ 深度 + 来源库标记。外部格式只出现在导入器里，运行时单一只读内部格式。
- 导入在 main 进程离线转换，进度可见，库文件级原子替换（失败丢弃半成品，不留脏状态）。

**部署与存储**：

- 库文件放用户数据目录 `books/`（与引擎目录并列）；每库一条清单记录（名称、来源、局面数、启用状态、优先级、校验和），卸载 = 删文件 + 清清单。
- **多库合并为一个结果列表使用**（参考 TCHESS `OpenBookManager`）：查询时按清单优先级顺序把各库命中依次追加为一个列表，不做全局去重择优——自动出招取列表首项（即最高优先级命中库的首选着法），库招表格展示全部命中，同招多库命中保留多行并标注来源；对调用方（对弈状态机/连线）表现为单一 OpeningBook。

**查询接口（main 进程 OpeningBook 服务，与 adapters 平级）**：

```ts
interface OpeningBook {
  lookup(pos: Position): Promise<BookMove[]>; // BookMove = { move; score; winRate?; wdl?; source }
}
```

- 唯一实现为本地 SQLite 只读查询，无网络依赖。winRate 与胜/和/负统计为可选字段：XQB 类库自带 W/D/L 可派生胜率，OBK/PF 类库只有分值。
- 出招编排在**对弈状态机**：先查库，命中且通过策略过滤 → 走库招专属随机延迟区间后落子（与 §6.1 拟人化延迟同机制，参考 TCHESS 的 bookDelay 区间）；未命中 → 引擎 genmove。连线模式复用同一通路，观战分析时库招与引擎线并列展示并标注来源。

**库招策略与脱谱规则（固定规则，无用户阈值）**：

- **引擎接管的两个条件**：① 库未命中；② 命中但**无正分着法**（最优候选分 ≤ 0，局面已亏不再吃库招）。
- 命中且有正分着法：**正分着法中等权随机取一**——阈值即 0，固定不可调（语义参考 TCHESS `POSITIVE_RANDOM`，但去掉"退化取最优"分支：非正分直接脱谱交引擎）；随机性天然防每局同型，对局内另记已走库招防循环。
- 分值口径为 cp（OBK/PF/XQB 均自带）；XQB 的胜/和/负统计与派生胜率仅用于展示，不参与脱谱判断。
- 库招策略独立于难度系统（§5.5）：放水由引擎弱化机制负责，库不掺和——与"分析输出诚实"同一原则。

## 9. 实施计划

| 阶段 | 内容                                                                                                                                                                                               | 验收                                           |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| P0   | Electron 脚手架、core 包骨架、CI 单测框架                                                                                                                                                          | 空窗口跑起来                                   |
| P1   | 象棋：Game 实现 + UciAdapter + 人机对弈 + 难度(UCI_Elo) + 胜率条（后演进为评估走势图，§7.4）                                                                                                       | 完整人机对弈闭环                               |
| P2   | 连线（象棋）：LinkerService(Win/mac) + 象棋识别模型 + 自动走棋（2026-08-25 提前：先全力做好象棋单棋种，围棋整体后移）                                                                              | 连线全流程可用                                 |
| P3   | 围棋：GoGame + GtpAdapter + Human SL 难度 + SGF + 让子/贴目 + 闲时思考（§5.9，两棋种默认关、对局中随时可开）                                                                                       | **验证抽象**（预留返工预算，它一定会打脸接口） |
| P4   | 连线（围棋）：识别 + 观战 + 引擎接管后一击交叉点                                                                                                                                                   | 识别/跟盘/代打可用（KaTrain / 本地测试靶）      |
| P5   | ~~主题（浅/深/跟随系统）、i18n 语言包（中/英/日）、音效~~（已提前完成 ✅ 2026-08-25）、引擎分发自动化之下载/回退链/镜像（**随包分发已提前完成 ✅**）、开局库（§8.1：预置/下载/导入安装、库招策略） | 发布候选                                       |

依赖关系：象棋先行——先把架构全链路跑通建立回归基准；围棋第二，用它的差异验证并修正接口设计。2026-08-25 调整：**一个棋种做透优先**——象棋连线（原 P3）提前为 P2，人机 + 连线构成象棋完整闭环后再启动围棋（原 P2 后移为 P3）。

### 9.1 实际进度（2026-08-25）

P0、P1 已完成并通过验收（P1 的"胜率条"在实施中演进为 §7.4 的评估走势折线图）。以下原属后续批次的内容已提前完成：

- **P4 连线（围棋）→ 识别+观战+代打（2026-08-29 / 2026-08-30）**：识别走经典 CV（`main/linker/go/`：等距线簇网格 + 圆盘中值分类 + 提子/劫/pass 解释），不引入模型。截图与点击复用象棋 LinkerNative（Win/mac）。LinkerSession 按棋种分派：开局默认观战（engineSide=null），工具栏执黑/执白后走同一套出招拦截器——本地先落子，再一击交叉点等平台跟上（虚着不点盘）。连线面板增棋种入口。本地测试靶 `scripts/dev-target-board.html?kind=go`；校验 `packages/app/scripts/go-recognition-verify.mjs`。真机首验对象 KaTrain（合成风格 fixture 已固化；窗口截图可补 `__fixtures__/go/`）。

- **P2 连线（象棋）→ 已完成（2026-08-25）**：LinkerService 端口层（mac desktopCapturer+nut.js 前台 / win PrintWindow+PostMessage 后台，平台耦合封顶 `main/linker/`）、YOLO 识别链路（TCHESS yolov11.onnx，**已决策接受 GPLv3 感染**，见 AGENTS.md）、LinkerSession 扫描循环（diff 唯一解释、翻转归一、动画确认两帧一致、新局 unknown 累计、pending-sync 点击重试、观战模式、⌘/Ctrl+Shift+X 急停）、连线面板 UI + 三语 + 参数设置、模型随包分发。识别端到端已在本地测试靶（`scripts/dev-target-board.html` + `packages/app/scripts/yolo-verify.mjs`）验证：初始与中局盘面 FEN 与真实局面一致；mac 真实平台与 Windows 后台模式留人工实测。同日按用户模型重构：**连线 = 以识别局面重开一局**（MatchService initialFen + 出招拦截器，见 §6.1），连线面板提供引擎执红/执黑换边按钮。

  **同日第二轮（走子准确性 + 失败恢复）**：真机对 TCHESS 实测暴露两类问题并已修复——
  ① 点击点算错（`gridClickPoint` 把"外扩 0.8 格"实现成"缩小格距"，边线最大偏 0.98 格，
  几乎落在相邻交叉点上）：改为精确格点 + 棋子中心自标定（§6.6），实测偏差降到 ≤0.02 格；
  截图与点击改为同源 anchor，修掉窗口拖动与越界 clamp 的偏移。
  ② 走子失败即终止：改为"自愈 → `attention` 待人工介入"闭环（§6.7），
  新增 `MatchService.playObserved`（平台是事实源）与 `setPaused`，修复拦截器拒绝后
  `thinking` 永不复位导致的**对局死锁**（引擎不走、用户也无法手动接管）。
  **macOS 后台模式定论（同日）**：连线的基线是"屏幕识别 + 真实点击"——唯一不需要目标
  配合、因而能兼容所有平台的手段，代价是目标必须可见、落子会动光标与焦点。在此基线上
  **macOS 无法后台落子**（三类目标 × 四种 API 变体 × 工具自检全部失败，见 §6.3），
  后台通路一律降格为"探测到才启用的加速层"。macOS 后台**取画面**则可行，价值在于
  让识别不再怕短暂遮挡。
  新增 `geometry.e2e.test.ts` 几何回归；`recognition.e2e.test.ts` 的 TCHESS 用例从
  "只打诊断"升级为硬断言——此前"模型对该界面风格泛化有限"是被 fixture 的全屏缩放误导的
  结论（生产链路抓的是窗口裁剪图），按同口径裁窗后 32 子全部 ≥0.94 置信度；
  也因此发现 `tchess-init.png` 其实不是初始局面（棋谱区记着已走"炮八平五 马2进3"）。

- **P5 → 已完成**：主题三态（nativeTheme + 浏览器 class 双通道）、i18n 三语资源包全量、音效（WebAudio）。
- **P5 引擎分发 → 部分完成**：安装包随包分发已做（`build-app` 双平台安装包：mac dmg arm64 / Windows nsis x64；`extraResources` 平台过滤，Windows 全 CPU 变体随包、`avx512icl` 优先命中，NNUE 同目录）；未做：默认下载/回退链/镜像。
- **超出原计划新增**：3D 棋盘（§7.2，默认开启可切 2D）、窗口置顶（§7.3 工具栏）、浏览器开发模式（§7.2）、应用打包与应用名（Super Go，dev 门面经 postinstall 改名）、暂停/继续与严格轮替对弈控制、快捷键体系、引擎互搏（engineSide='both'）、对弈设置固化（棋力五模式为固有配置）。
- **范围修正**：象棋棋谱导入导出确认砍掉（§8）；双方信息条不做（§7.1）；应用菜单自建（mac 本地化 role，dev 模式菜单栏/Dock 显示 Super Go）。

## 10. 风险与开放问题

| #   | 项                                                    | 状态                                                                                                                   |
| --- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1   | KataGo Human SL 段位的运行时切换 GTP 命令是否存在     | 待核实 `docs/Analysis_Engine.md`（Human SL Analysis Guide）；若无 → 切档重启进程，代价可忽略                           |
| 2   | 皮卡鱼强度参数                                        | 已核实（官方 Wiki）：UCI_Elo 1280–3133 天梯校准；运行时再跑一次 `uci` 实测确认即可                                     |
| 3   | 围棋 YOLO 模型来源                                    | **关闭（2026-08-29）**：改经典 CV，不引入模型。见 §6.4                                                                   |
| 4   | macOS 三权限的引导体验                                | 首次连线时弹引导页 + 检测授权状态                                                                                      |
| 5   | 围棋连线自动落子的误识别后果                          | 本地规则校验（提子集合 / 劫 / isLegal）拦截误识别；点不中走自愈重试，耗尽转 `attention` 人工补一手即可恢复              |
| 6   | 产品名                                                | 已定 **Super Go**（2026-08-25：productName/setName/dev 门面改名一致）                                                  |
| 7   | `kata-set-position` 可用性                            | 以实际版本 GTP `list_commands` 为准；fallback = 连线模块维护着法历史重放                                               |
| 8   | OBK/PF/XQB 为社区逆向的非公开格式，字段细节无权威文档 | 导入器按实测样本建兼容矩阵；内部格式不依赖外部细节，单个库导入失败不影响其他功能                                       |
| 9   | KataGo pondering 能否运行时切换（对局中开关）         | `kata-set-param` 未收录 pondering 项，以 `kata-list-params` 实测为准；fallback = 重启 + 快照重同步（§5.8），代价可忽略 |

## 11. 参考项目

**TCHESS**（单棋种象棋界面程序，GPLv3）：`~/devs/public-Xiangqi`

本项目是全新实现，与其无代码继承关系，但以下内容可作为实现参考：

- `XiangqiUtils`（~1000 行）：象棋规则/坐标/记谱/FEN 完整算法，可翻译成 TS（P1 提速的主要来源）
- `ZobristUtils`（~800 行）：局面哈希实现
- `Engine.java`：UCI 引擎进程管理骨架
- `yolov11.onnx` 及其后处理：象棋棋子识别模型，可直接复用
- 棋谱四格式解析（XQF/CBR/PGN/TXQ）、本地开局库（openbook 包：`OpenBookManager` 多库聚合、`OpenBook` 四档选择策略与库招延迟、OBK/PF/XQB 三格式 reader、XQB 位压缩局面键与镜像归一）：现成参考
- 连线的窗口定位、动画确认、参数体系：设计已吸收进本文档 §6

注意：该项目采用 GPLv3，若翻译或复用其代码/模型，本项目需遵守相同许可证。仓库许可证与第三方声明见 [`LICENSE`](./LICENSE)、[`NOTICE.md`](./NOTICE.md)。
