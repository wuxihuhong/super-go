# AGENTS.md — super-go

## 项目说明

双棋种（围棋 + 象棋）对弈工具，两条核心场景共用一条引擎通路（同步局面 → 要一步棋 → 拟人化延迟 → 落子）：

1. **本机人机对弈**：引擎执一方，难度可选（象棋等级分 / 围棋段位）。
2. **连线代打**：屏幕识别第三方平台棋盘 → 引擎出招 → 模拟鼠标自动落子。

**权威设计文档为 `DESIGN.md`**，改动任何核心模块前先读对应章节：core(§4)、引擎层(§5)、连线(§6)、UI(§7)、棋谱(§8)。

## 架构设计

三层结构，边界铁律：

- **renderer**：UI（棋盘渲染 / 对局控制 / 引擎管理 / 连线交互）。
- **main**：引擎子进程、识别推理、原生 IO 全部在此进程，数据经 IPC 事件推给 renderer。
- **core**：纯逻辑（Game 接口、MoveTree、对弈状态机、规则/记谱/Zobrist），**零 Electron 依赖**。

- `LinkerService`（§6.2）是**全项目唯一允许平台耦合的模块**，core 与 UI 只依赖其接口。
- `Game` 接口多态设计（Move/Position/diffPositions），**必须同时适配两棋种**，只按单一棋种设计接口是禁止的。
- 局面同步统一为快照式 `syncPosition`：UCI 全量重发 `position fen ... moves ...`；GTP `clear_board` + 重放，**不依赖 `undo`**。
- 引擎抽象 `EngineAdapter`：UciAdapter(Pikafish) / GtpAdapter(KataGo)；评估分多态 `ScoreKind: cp | winRate`，统一挂 MoveTree 的 EvalRecord。

## 代码规范

- 全 TypeScript；技术栈已定案勿替换：Electron、nut.js（鼠标监听/注入）、koffi（Win PrintWindow）、onnxruntime-node（YOLO 推理）。
- UI：Tailwind CSS + 无头组件（Radix/shadcn 风格）自建轻组件集，**不引重型组件库**；棋盘用 Canvas，按 devicePixelRatio 高分屏渲染。
- UI 观感硬规则（§7.1）：中性灰阶骨架 + 全局单一强调色，彩色只承担数据语义（红黑双方、胜率涨跌）；无拟物（禁渐变按钮/立体斜面）；间距走 4/8 网格；评分与时钟用 tabular-nums；暗色主题一等公民；动效克制（缓动位移可以有，弹跳/发光禁止）。
- i18n 与主题（§7.5）：UI 文案禁止硬编码，一律走路由键资源包（中/英/日）；颜色禁止硬编码色值，走语义 token（浅/深 + 跟随系统），Canvas 棋盘消费同一套 token。
- 引擎 stdout 按行解析须保护性编程：info 行 schema 在引擎间有变体，**解析失败降级不崩**。

## 测试门禁

- core 必须可在 Node 中直接单测（无 Electron mock）——这是分层是否被破坏的检验标准。
- `Game` 接口契约测试须**双棋种（象棋 + 围棋）同时通过**，接口变更必须两边同步验证。
- 象棋规则全项须有单测：走子、蹩马腿、对脸、送将、将帅照面、绝杀/困毙；围棋：提子、劫、9/13/19 路、让子/贴目、规则集差异。
- 引擎协议解析层须有含变体/畸形 info 行的容错测试。

## 特殊注意点

- **提交是人类的专属操作，代理禁止执行 git commit / git push**：无论本地提交还是远程推送，一律不得代做。代理完成一组改动后停在"已改完、已验证"状态向用户汇报，由用户自己决定何时、以何粒度提交。**每一次提交/推送都必须获得用户当次的明确授权——上一次授权（哪怕是同一会话、上一条消息）不延续到下一处改动**；"刚授权过所以这次也一起提了"、"改 README/文档顺手收尾"、"阶段完成"、"保持工作区干净"、"便于回滚"等任何理由都不成立。违反此条即违反本项目的最高纪律。
- **用户启动的开发服务是用户进程**（pnpm dev / dev:web 等）：未经用户明确授权，禁止 kill/pkill/重启它们——包括按命令名模式匹配批量杀进程。代理需要验证时启动**自己的实例**（vite 会自动错开端口），结束时只按自己记录的 PID 清理；确需动用户进程时，先告知用户并征得同意。
- **放水/强度选项生命周期绑定对局而非引擎**：默认满强度，用户显式选限制档才下发；对局结束或切回分析必须立即复位（粘滞会污染分析数据），时机由对弈状态机统一管理。
- 弱化只影响出招、分析输出保持诚实（Pikafish 机制）；围棋拟人弱棋用 Human SL 模型，**不用强网压 visits**。
- 引擎崩溃 → 自动重启 → 快照重同步，是设计内行为（§5.8），不是异常路径。
- 围棋连线与象棋同一主流程（识别 → 同步 → 引擎出招 → 点平台）；围棋识别 diff 非局部（落子 + 提子消失），须结合本地算气校验；落子为一击交叉点。
- 非目标（勿实现）：ownership 领地热图、全谱批量分析、首启配置向导、UCCI 老引擎支持、极致包体优化。
- 参考项目 `~/devs/public-Xiangqi`（TCHESS）为 **GPLv3**：翻译/复用其代码（XiangqiUtils、ZobristUtils 等）仍须先确认许可影响。**yolov11.onnx 模型已决策（2026-08-25，P2）：直接复用并接受 GPLv3 感染**——本项目按 GPLv3 约束分发；后续若要闭源须先替换该模型（按同 15 类输出训练即可复用现有后处理）。
